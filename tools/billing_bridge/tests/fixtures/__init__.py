# Billing bridge test fixtures — organized by spreadsheet type.
#
# Each sub-package exposes a `factory` module with randomized DataFrame
# generators so tests can cover many input combinations without
# hand-writing every scenario.

