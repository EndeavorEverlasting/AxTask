import { describe, it, expect } from "vitest";

describe("legacy test suite (opt-in)", () => {
  it("placeholder — add deprecated-flow tests here and run `npm run test:legacy`", () => {
    expect(true).toBe(true);
  });
});
