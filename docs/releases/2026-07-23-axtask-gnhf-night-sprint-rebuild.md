# AxTask GNHF Night Sprint Rebuild

**Date:** 2026-07-23  
**Source:** unique work preserved from stale PR #81

## Delivered

- one-click directory-first Windows CMD launcher;
- repository-owned PowerShell launcher using AgentSwitchboard's merged provider-routed GNHF control plane;
- bounded AxTask overnight runtime objective aligned with the current `.ai` harness and collision inspection;
- deterministic validator and normal Vitest discovery;
- operator runbook and morning review procedure.

## Preservation repair

The stale PR branch was not merged wholesale. The files were rebuilt on current `main`, the AgentSwitchboard dependency was updated from branch language to merged PR #25, and the runtime objective now includes current harness authority and collision inspection.

## Runtime impact

No AxTask application runtime, schema, migration, deployment, authentication, scheduled resource, dependency, or production behavior changes. The new behavior is invoked only when an operator runs the local CMD or PowerShell launcher.

## Rollback

Revert the launcher, objective, runbook, validator, test, Vitest discovery entry, and this release record. No data rollback is required.

## Proof ceiling

Repository validation can prove tracked files, command shape, safety contracts, and build compatibility. It does not prove local provider authentication, quota, network, an actual overnight run, generated worktree delivery, push, merge, deployment, or production behavior.
